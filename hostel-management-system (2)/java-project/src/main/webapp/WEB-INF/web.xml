<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <display-name>Hostel Management System</display-name>

    <!-- 1. Entry Welcome File List -->
    <welcome-file-list>
        <welcome-file>login.jsp</welcome-file>
    </welcome-file-list>

    <!-- 2. Administrative Security Session Timeouts -->
    <session-config>
        <session-timeout>30</session-timeout> <!-- 30 Minutes default idle lock -->
    </session-config>

    <!-- 3. Tomcat JNDI Resource Pooling Reference declarations -->
    <resource-ref>
        <description>Central MySQL Database Connection Pool for Hostel Manager</description>
        <res-ref-name>jdbc/HostelDB</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>

    <!-- 4. Error Forwarding Configuration pages -->
    <error-page>
        <error-code>404</error-code>
        <location>/login.jsp</location> <!-- Fallback unauthorized or unknown nodes safely -->
    </error-page>

</web-app>
